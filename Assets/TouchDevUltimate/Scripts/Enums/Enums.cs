namespace TouchDevUltimate
{
    public enum InputState
    {
        Down,
        Pressed,
        Up
    }

    public enum GestureType
    {
        Tap,
        SwipeUp,
        SwipeDown,
        SwipeLeft,
        SwipeRight
    }

    public enum CharacterType { Player, AI }

    public enum BTNodeState { Failure, Success, Running }
}